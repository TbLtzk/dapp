export const aliasesSelector = (state) => state.accountAliases.aliases;
export const aliasesLoadingSelector = (state) => state.accountAliases.isAliasesLoading;

export const aliasEventsSelector = (state) => state.accountAliases.events;
export const aliasEventsLoadingSelector = (state) => state.accountAliases.isEventsLoading;
