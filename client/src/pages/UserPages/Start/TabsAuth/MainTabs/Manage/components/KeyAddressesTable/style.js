import styled from 'styled-components';

export const KeyAddressesTableWrap = styled.div`
  .table {
    margin-top: 20px;
  }
  .key-addresses-table-wrap__address {
    color: ${props => props.theme.colors.black};
  }

`;
